name: stocksim
region: fra
services:
  - name: frontend
    environment_slug: node-js
    build_command: npm install && npm run build -- --configuration production
    run_command: ''
    output_dir: dist/your-project-name
    routes:
      - path: /*
        rewrite: /index.html